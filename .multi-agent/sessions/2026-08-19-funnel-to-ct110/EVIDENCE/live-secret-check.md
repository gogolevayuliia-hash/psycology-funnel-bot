# Проверка секрета на настоящем HTTP-сервере (до merge)

Запущен uvicorn с новым кодом на 127.0.0.1:8757 (порт проверен на занятость —
первая попытка на 8099 попала в чужой сервер и дала ложные 200, результат отброшен).

```
сервер мой, health: {"status":"ok","bot":"funnel"}
без заголовка:    HTTP 403  (ждём 403)
неверный секрет:  HTTP 403  (ждём 403)
верный секрет:    HTTP 200  (ждём 200)
--- setWebhook при пустом PUBLIC_URL ---
WARNING:main:PUBLIC_URL not set — webhook not configured
```

Последняя строка — предпосылка К9: ровно так поведёт себя копия на Railway после merge,
где `PUBLIC_URL` не задан. Вебхук она не тронет.
